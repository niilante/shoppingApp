@{
    ViewBag.Title = "Home Page";
}
@model List<Blog.Models.BlogPost>

<!-- Top Navigation -->

<section id="hero" class="light-typo">
    <div id="cover-image" class="image-bg animated fadeIn"></div>
    <div class="container welcome-content">
        <div class="middle-text">
            <h1>Key Lime Pie Blog</h1>
            <h2><b>Key Lime Pie Blog</b> is a blog theme built with Bootstrap</h2>
        </div>
    </div>
</section>

@foreach (var blogPost in Model)
{
    <div id="start" class="container content">
        <div class="row">
            <div class="col-md-1"></div>
            <div class="col-md-10">
                <article class="clearfix">
                    <h2>@blogPost.Title</h2>
                    <div class="post-date">
                        @blogPost.Created | @blogPost.CreatedBy.FirstName @blogPost.CreatedBy.LastName                                              
                    </div>
                    <p>
                        @*@blogPost.Body @Html.ActionLink("Details", "Index", "BlogPosts")*@
                        @Html.Raw(blogPost.Body)
                    </p>
                    <p>
                        @Html.ActionLink("Details", "Details", "BlogPosts", new { slug = blogPost.Slug }, null)
                    </p>
                </article>
            </div>
            <div class="col-md-1"></div>
        </div>
    </div>
}


    



